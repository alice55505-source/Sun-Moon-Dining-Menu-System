{
  "name": "sun-moon-dining-menu-system",
  "version": "0.1.0",
  "description": "日月自助餐出菜單系統 - 合菜訂單、月菜單、客製化菜單與採購管理（Cloudflare Pages + D1）",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev",
    "deploy": "wrangler pages deploy public",
    "seed:generate": "node scripts/generate-seed-sql.js",
    "seed:apply:local": "wrangler d1 execute sun-moon-dining-menu-db --local --file=migrations/0001_schema.sql && wrangler d1 execute sun-moon-dining-menu-db --local --file=migrations/0002_seed.sql",
    "seed:apply:remote": "wrangler d1 execute sun-moon-dining-menu-db --remote --file=migrations/0001_schema.sql && wrangler d1 execute sun-moon-dining-menu-db --remote --file=migrations/0002_seed.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
